import React from 'react';
import { Typography } from '@material-ui/core';

const Header = () => {
	return (
		<div>
			<Typography>All invoices - 2016</Typography>
		</div>
	);
};

export default Header;
